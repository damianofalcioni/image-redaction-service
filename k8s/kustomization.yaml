apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  # Enable only after changing the host and ingressClassName.
  # - ingress.yaml
images:
  - name: ghcr.io/YOUR_ORG/image-redaction-service
    newTag: 0.4.0
